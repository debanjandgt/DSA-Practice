class Solution {
    public int strStr(String haystack, String needle) {
        char c1=needle.charAt(0);
        int pos=-1;
        for(int i=0;i<haystack.length();i++){
            S1+=(haystack.charAt(i));
            if(S1.equals(needle))
        }
    }
}
